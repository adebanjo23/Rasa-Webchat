    const config = {
        useSocket: true, // Change to true to use Socket.io, false to use REST
        restEndpoint: 'http://localhost:5005/webhooks/rest/webhook',
        socketEndpoint: 'http://localhost:8000',
    };
